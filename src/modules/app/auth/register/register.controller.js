
'use restrict';

var dependencies = [ '$scope', '$rootScope', 'AUTH_EVENTS', 'AuthService' ],
    RegisterController = function($scope, $rootScope, AUTH_EVENTS, AuthService) {

      // @todo implement register controller

    };

module.exports = dependencies.concat(RegisterController);
