'use strict';

var constant = {
  /** this will be removed in the future **/
  all: '*',
  admin: 'admin', /** we dont limit pages but we limit actions to certain roles **/
  player: 'player',
  guest: 'guest'
};

module.exports = constant;
