'use strict';

var dependencies =[ 'PlayerService' ],
    BlockController = function(PlayerService) {

      // do something in the player block  or not..

    };

module.exports = dependencies.concat(BlockController);
